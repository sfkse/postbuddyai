// ENUMS
export enum EUserRoles {
  ADMIN = "ADMIN",
  USER = "USER",
  GUEST = "GUEST",
}

export enum ECampaignStatus {
  ACTIVE = 1,
  DELETED = 2,
}

export enum ETweetStatus {
  ACTIVE = 1,
  SENT = 2,
  DRAFT = 3,
  DELETED = 4,
}

